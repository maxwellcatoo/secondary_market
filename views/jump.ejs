<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>发布成功</title>
</head>
<body>
  <p>发布成功，<span id="time">5</span>秒后跳转到发布信息页</p>
  <script>
    let time = document.getElementById('time')
    let seconds = 5
    setInterval(() => {
      if(seconds <= 0){
        location.assign('http://localhost:8081/new')
      }else {
        time.textContent = --seconds
      }
    }, 1000);

    
      // location.assign('http://localhost:8081/new/')
  </script>
</body>
</html>
